#include<stdio.h>
int main() 
{
    int num, first_digit, last_digit, sum;
    printf("Enter three_digit +ve integer: ");
    scanf("%d", &num);
    first_digit = num / 100;
    sum = firstd_digit + last_digit;
    printf("\n %d The sum of first & last digit is: ", sum);
    return 0;
}